# See `shared/src/serverEnv.ts` + `shared/src/clientEnv.ts` for 
# validation rules.

NODE_ENV=development

# Used by both the Express server and the Vite dev proxy
VITE_EXPRESS_PORT=3001

# MongoDB connection string
DATABASE_URL=

# Optional public vars for the app
# VITE_API_URL=http://localhost:3001
